# build:
#   docker:
#     web: ./nginx/Dockerfile
# run:
#   web: bundle exec puma -C config/puma.rb

# build:
#   docker:
#     web: ./backend/Dockerfile
# run:
#   web: bundle exec puma -C config/puma.rb

setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: ./frontend/Dockerfile
    worker: ./backend/Dockerfile
release:
  command:
    - ./start.sh
  image: worker
  command:
    - npm run serve --prefix /dist
  image: web
# run:
#   web: bundle exec puma -C config/puma.rb
#   worker: python3 manage.py runserver
# run:
#   web: bundle exec puma -C config/puma.rb
#   worker: python manage.py
