build:
  docker:
    web: Dockerfile.prod
run:
  web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
# ! NOTE: Active seulement le backend
# run:
#   web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
